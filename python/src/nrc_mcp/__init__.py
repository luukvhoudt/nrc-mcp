"""nrc-mcp — an MCP server for authoring, analysing and shipping NetRadiant-custom levels.

The kernel owns the `.map`; this package exposes it to an agent. See `server.py` for the
tool surface and `docs/spec-corrections.md` for design claims that did not survive
verification.
"""

__version__ = "0.1.0"
